# Semantic content release: 11 September 2026

## Brief

Audience: website owners, SEO practitioners and agency teams moving from an observed website problem to an implementable finding. The priority action is downloading a useful report template or completed example and following the corresponding diagnostic/reporting method.

Preserve Astro, ContentLayout, the navy/cyan palette and existing typography. The content is a working audit document: specific finding IDs, evidence dates and owner/retest records are the subject-specific visual anchors. Put the two working-file actions beneath the report title. Use readable finding sections instead of a wide matrix of long paragraphs. Maintain one page task and clear routes between audit scope, diagnostic method, reporting method, completed example and blank files.

The completed example concerns AuditWeb's own documented defects. It includes no client data and makes no ranking or revenue claim. The separate historical case study remains the source narrative. Existing service prices and enquiry behaviour are outside this change.

## Method

- Apply semantic-brief, content-writer, semantic-audit, topical-map and internal-link-mapper to existing URL ownership and content.
- Prefer direct answers, question-led sections, concrete evidence and concise paragraphs. Word count is not a ranking target.
- Allow the few early contextual links and download actions needed to perform the page task. Do not apply a mechanical first-ten-headings link ban that would hide useful resources.
- Preserve accurate qualifications where Google data cannot support certainty. Treat automated editorial checks as checks of structure, not evidence of Google's ranking decisions.
- Use Google's primary documentation for Search Console dimensions, URL Inspection, core updates, manual actions and disavow limits. Link sources close to the relevant guidance.

## Validation record

Lower-cost implementation agents produced the content and contextual-link changes. A separate reviewer checked the three substantial rewrites against their distinct page tasks, owned-site evidence and primary Google documentation. Review corrections addressed core-update comparison timing, click definitions, recovery limits, download status and TOC coverage. No numeric semantic or ranking score is claimed.

The supported Node 22 build produces 179 static pages. The rendered HTML validator reports no errors or warnings, and the output link/anchor/JSON-LD check reports no issues. All 22 browser regression tests passed, including keyboard downloads and the diagnostic-to-report journey. Browser checks cover the nine edited pages at 1440, 390 and 320 pixels: no horizontal overflow, automated WCAG violations, console errors or failed resource requests. Initial font-swap shifts were corrected with `font-display: optional`; the checked article pages then recorded zero lab layout shift. These lab observations are not field Core Web Vitals results.

The dependency audit reports zero known vulnerabilities. A source and built-output credential-pattern scan reports no matches; sensitive-file history checks find no tracked credentials. Private baseline data and detailed screenshots are stored outside the public repository. Live deployment readback remains a post-publication check and is recorded in the private release evidence.

The linked topical-map skill's historical `SEO_Agents/01_Project_Manager_Agent_Instructions.md` reference is absent from the workspace. The available topical-map instructions and actual site inventory are used; no missing methodology is represented as read.
